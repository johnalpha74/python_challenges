word = "Victoria BC"
print(word[8:11]) # print characters from position 8 (included) to 11 (excluded)

#Slice indices have useful defaults; an omitted first index defaults to zero, an omitted second index defaults to the size of the string being sliced.
word = "Canada"
print(word[4:]) # print characters from position 4 (included) to the end
